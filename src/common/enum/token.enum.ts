//* TokenTypeEnum defines the types of tokens used in the application, including access tokens and refresh tokens.
export enum TokenTypeEnum {
  access_token = 'access_token',
  refresh_token = 'refresh_token',
}
